{
  "mcpServers": {
    "mysql": {
      "command": "mcp-mysql",
      "args": [
        "--host", "localhost",
        "--port", "3306",
        "--user", "mbti_user",
        "--database", "mbti_test",
        "--password", "MbtiPass123!"
      ]
    }
  }
}